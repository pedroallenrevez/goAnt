package main

import "fmt"

func main() {
	//init world
	/*
	   var a IFace
	   a = Create()
	   a.SetSomeField("World")
	   fmt.Println(a.GetSomeField())
	*/
	//init k ants(threads)
	//start ant threads
	//join threads
	//decay map
	//updateWorld with ant routes ( shortest dist, pheromone)
	//ant reset
}
